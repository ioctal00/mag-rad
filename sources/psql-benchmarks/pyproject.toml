[project]
name = "psql-benchmarks"
version = "0.1.0"
description = "Benchmark and experiment harness for thesis PostgreSQL/Citus workloads"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.11",
]
