#cloud-config
package_update: true
package_upgrade: true
packages:
  - python3
  - python3-apt
  - python3-pip
  - curl
  - ca-certificates
  - jq
  - sudo
  - acl
write_files:
  - path: /etc/motd
    permissions: "0644"
    owner: root:root
    content: |
      Thesis infrastructure node: regional backend
      Provisioned by Terraform. Configure services with Ansible.
runcmd:
  - [systemctl, daemon-reload]
