select tenant_id,
       count(*) as events_count,
       sum(value) as total_value
from events
where created_at >= now() - make_interval(days => {{ lookback_days }}::int)
  and value >= {{ min_value }}::double precision
group by tenant_id
order by total_value desc, tenant_id;
