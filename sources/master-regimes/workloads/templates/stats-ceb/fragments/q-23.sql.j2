select count(*) as result_count
FROM {{ schema_name }}.votes AS v, {{ schema_name }}.badges AS b, {{ schema_name }}.users AS u
WHERE u.Id = v.UserId
  AND v.UserId = b.UserId
  AND u.DownVotes >= 0
  AND u.DownVotes <= 0
