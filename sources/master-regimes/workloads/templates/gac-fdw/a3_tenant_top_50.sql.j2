with all_events as (
  select * from fdw_eu.events
  union all
  select * from fdw_us.events
)
select tenant_id, sum(value) as total_value
from all_events
where created_at >= timestamptz '{{ from_ts_tz }}'
group by tenant_id
order by total_value desc, tenant_id
limit {{ limit_rows }};
